// Code generated by entpoly. DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"strconv"
	"time"

	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
	"github.com/khanakia/entx/testentpoly/ent/annotation"
	"github.com/khanakia/entx/testentpoly/ent/comment"
	"github.com/khanakia/entx/testentpoly/ent/document"
	"github.com/khanakia/entx/testentpoly/ent/event"
	"github.com/khanakia/entx/testentpoly/ent/folder"
	"github.com/khanakia/entx/testentpoly/ent/image"
	"github.com/khanakia/entx/testentpoly/ent/post"
	"github.com/khanakia/entx/testentpoly/ent/predicate"
	"github.com/khanakia/entx/testentpoly/ent/report"
	"github.com/khanakia/entx/testentpoly/ent/tag"
	"github.com/khanakia/entx/testentpoly/ent/taggable"
	"github.com/khanakia/entx/testentpoly/ent/video"
)

// Compile-time silence for the sql import when no per-parent
// predicate constructor (On<Parent>) is emitted. The compiler trims
// dead code; this var avoids "imported and not used" on a graph that
// happens to have no polymorphic children with ResolveCases.
var _ = sql.Select
var _ = time.Now

// Compile-time silence in case strconv is never referenced (rare — only
// when no child resolver branch needs numeric ID parsing).
var _ = strconv.Atoi

// Compile-time silence in case context is never referenced (rare — only
// when no MorphOne back-refs are present in the entire graph).
var _ = context.Background

// MorphKey is a named string type for the persisted "*_type" alias of
// every parent type in a polymorphic relation. Using a named type instead
// of a raw string lets callers pass only one of the codegen-emitted
// constants (PostMorphKey, VideoMorphKey, ...), which the Go compiler
// validates — preventing typos like CommentableTypeEQ("psot").
type MorphKey string

// Morphable is implemented by every entity that may sit on the parent side
// of a polymorphic relation. Generated MorphID()/MorphKey() satisfy this.
type Morphable interface {
	MorphID() string
	MorphKey() MorphKey
}

// ─── Morph map ─────────────────────────────────────────────────────────────

// morphTypeMap maps the persisted "*_type" string to the ent type name.
// Configure aliases via entpoly.WithMorphMap(...) at codegen time.
var morphTypeMap = map[MorphKey]string{
	DocumentMorphKey: "Document",
	FolderMorphKey:   "Folder",
	ImageMorphKey:    "Image",
	PostMorphKey:     "Post",
	ReportMorphKey:   "Report",
	VideoMorphKey:    "Video",
}

// MorphTypeFor returns the morph key registered for an ent type name, or
// an empty string when no alias is registered.
func MorphTypeFor(typeName string) MorphKey {
	for k, v := range morphTypeMap {
		if v == typeName {
			return k
		}
	}
	return ""
}

// MorphTypeName returns the ent type name registered for a morph key, or
// an empty string when the key is unknown.
func MorphTypeName(morphKey MorphKey) string {
	return morphTypeMap[morphKey]
} // Per-type morph-key constants. Use these instead of hard-coding the
// persisted string — the named MorphKey type prevents arbitrary string
// literals from being passed where a polymorphic alias is expected.
//
//	client.Comment.Query().Where(
//	    CommentCommentableIsType(PostMorphKey),  // typed: only known constants compile
//	).All(ctx)
const (
	// DocumentMorphKey is the persisted "*_type" alias for Document.
	DocumentMorphKey MorphKey = "document"
	// FolderMorphKey is the persisted "*_type" alias for Folder.
	FolderMorphKey MorphKey = "folder"
	// ImageMorphKey is the persisted "*_type" alias for Image.
	ImageMorphKey MorphKey = "image"
	// PostMorphKey is the persisted "*_type" alias for Post.
	PostMorphKey MorphKey = "post"
	// ReportMorphKey is the persisted "*_type" alias for Report.
	ReportMorphKey MorphKey = "report"
	// VideoMorphKey is the persisted "*_type" alias for Video.
	VideoMorphKey MorphKey = "video"
)

// MorphID returns the string id for Document. Non-string ids are stringified
// via fmt.Sprint to fit the polymorphic id column.
func (e *Document) MorphID() string { return fmt.Sprint(e.ID) }

// MorphKey returns the persisted "*_type" alias for Document.
func (*Document) MorphKey() MorphKey { return DocumentMorphKey }

// MorphID returns the string id for Folder. Non-string ids are stringified
// via fmt.Sprint to fit the polymorphic id column.
func (e *Folder) MorphID() string { return fmt.Sprint(e.ID) }

// MorphKey returns the persisted "*_type" alias for Folder.
func (*Folder) MorphKey() MorphKey { return FolderMorphKey }

// MorphID returns the string id for Image. Non-string ids are stringified
// via fmt.Sprint to fit the polymorphic id column.
func (e *Image) MorphID() string { return fmt.Sprint(e.ID) }

// MorphKey returns the persisted "*_type" alias for Image.
func (*Image) MorphKey() MorphKey { return ImageMorphKey }

// MorphID returns the string id for Post. Non-string ids are stringified
// via fmt.Sprint to fit the polymorphic id column.
func (e *Post) MorphID() string { return fmt.Sprint(e.ID) }

// MorphKey returns the persisted "*_type" alias for Post.
func (*Post) MorphKey() MorphKey { return PostMorphKey }

// MorphID returns the string id for Report. Non-string ids are stringified
// via fmt.Sprint to fit the polymorphic id column.
func (e *Report) MorphID() string { return fmt.Sprint(e.ID) }

// MorphKey returns the persisted "*_type" alias for Report.
func (*Report) MorphKey() MorphKey { return ReportMorphKey }

// MorphID returns the string id for Video. Non-string ids are stringified
// via fmt.Sprint to fit the polymorphic id column.
func (e *Video) MorphID() string { return fmt.Sprint(e.ID) }

// MorphKey returns the persisted "*_type" alias for Video.
func (*Video) MorphKey() MorphKey { return VideoMorphKey }

// AnnotationTargetParent is the sealed interface restricting
// which types may be passed as the polymorphic parent of a Annotation on
// its "target" relation. Only types listed in the schema's
// AllowedTypes (passed to MorphTo) carry the marker method, so passing
// any other type to SetTarget is a compile error.
//
// This is the Go-equivalent of a sum type / discriminated union; it mirrors
// the way ent's typed enum fields restrict assignable values.
type AnnotationTargetParent interface {
	Morphable
	isAnnotationTargetParent()
}

// isAnnotationTargetParent marks *Document as an
// allowed parent of Annotation on the "target" relation.
// Generated only for types listed in the schema's AllowedTypes; this is
// the entire mechanism behind the compile-time restriction.
func (*Document) isAnnotationTargetParent() {}

// isAnnotationTargetParent marks *Report as an
// allowed parent of Annotation on the "target" relation.
// Generated only for types listed in the schema's AllowedTypes; this is
// the entire mechanism behind the compile-time restriction.
func (*Report) isAnnotationTargetParent() {}

// AnnotationSecondaryParent is the sealed interface restricting
// which types may be passed as the polymorphic parent of a Annotation on
// its "secondary" relation. Only types listed in the schema's
// AllowedTypes (passed to MorphTo) carry the marker method, so passing
// any other type to SetSecondary is a compile error.
//
// This is the Go-equivalent of a sum type / discriminated union; it mirrors
// the way ent's typed enum fields restrict assignable values.
type AnnotationSecondaryParent interface {
	Morphable
	isAnnotationSecondaryParent()
}

// isAnnotationSecondaryParent marks *Post as an
// allowed parent of Annotation on the "secondary" relation.
// Generated only for types listed in the schema's AllowedTypes; this is
// the entire mechanism behind the compile-time restriction.
func (*Post) isAnnotationSecondaryParent() {}

// isAnnotationSecondaryParent marks *Video as an
// allowed parent of Annotation on the "secondary" relation.
// Generated only for types listed in the schema's AllowedTypes; this is
// the entire mechanism behind the compile-time restriction.
func (*Video) isAnnotationSecondaryParent() {}

// CommentCommentableParent is the sealed interface restricting
// which types may be passed as the polymorphic parent of a Comment on
// its "commentable" relation. Only types listed in the schema's
// AllowedTypes (passed to MorphTo) carry the marker method, so passing
// any other type to SetCommentable is a compile error.
//
// This is the Go-equivalent of a sum type / discriminated union; it mirrors
// the way ent's typed enum fields restrict assignable values.
type CommentCommentableParent interface {
	Morphable
	isCommentCommentableParent()
}

// isCommentCommentableParent marks *Post as an
// allowed parent of Comment on the "commentable" relation.
// Generated only for types listed in the schema's AllowedTypes; this is
// the entire mechanism behind the compile-time restriction.
func (*Post) isCommentCommentableParent() {}

// isCommentCommentableParent marks *Video as an
// allowed parent of Comment on the "commentable" relation.
// Generated only for types listed in the schema's AllowedTypes; this is
// the entire mechanism behind the compile-time restriction.
func (*Video) isCommentCommentableParent() {}

// isCommentCommentableParent marks *Image as an
// allowed parent of Comment on the "commentable" relation.
// Generated only for types listed in the schema's AllowedTypes; this is
// the entire mechanism behind the compile-time restriction.
func (*Image) isCommentCommentableParent() {}

// EventEntityParent is the sealed interface restricting
// which types may be passed as the polymorphic parent of a Event on
// its "entity" relation. Only types listed in the schema's
// AllowedTypes (passed to MorphTo) carry the marker method, so passing
// any other type to SetEntity is a compile error.
//
// This is the Go-equivalent of a sum type / discriminated union; it mirrors
// the way ent's typed enum fields restrict assignable values.
type EventEntityParent interface {
	Morphable
	isEventEntityParent()
}

// isEventEntityParent marks *Post as an
// allowed parent of Event on the "entity" relation.
// Generated only for types listed in the schema's AllowedTypes; this is
// the entire mechanism behind the compile-time restriction.
func (*Post) isEventEntityParent() {}

// isEventEntityParent marks *Video as an
// allowed parent of Event on the "entity" relation.
// Generated only for types listed in the schema's AllowedTypes; this is
// the entire mechanism behind the compile-time restriction.
func (*Video) isEventEntityParent() {}

// FolderParentParent is the sealed interface restricting
// which types may be passed as the polymorphic parent of a Folder on
// its "parent" relation. Only types listed in the schema's
// AllowedTypes (passed to MorphTo) carry the marker method, so passing
// any other type to SetParent is a compile error.
//
// This is the Go-equivalent of a sum type / discriminated union; it mirrors
// the way ent's typed enum fields restrict assignable values.
type FolderParentParent interface {
	Morphable
	isFolderParentParent()
}

// isFolderParentParent marks *Folder as an
// allowed parent of Folder on the "parent" relation.
// Generated only for types listed in the schema's AllowedTypes; this is
// the entire mechanism behind the compile-time restriction.
func (*Folder) isFolderParentParent() {}

// isFolderParentParent marks *Document as an
// allowed parent of Folder on the "parent" relation.
// Generated only for types listed in the schema's AllowedTypes; this is
// the entire mechanism behind the compile-time restriction.
func (*Document) isFolderParentParent() {}

// TaggableTaggableParent is the sealed interface restricting
// which types may be passed as the polymorphic parent of a Taggable on
// its "taggable" relation. Only types listed in the schema's
// AllowedTypes (passed to MorphTo) carry the marker method, so passing
// any other type to SetTaggable is a compile error.
//
// This is the Go-equivalent of a sum type / discriminated union; it mirrors
// the way ent's typed enum fields restrict assignable values.
type TaggableTaggableParent interface {
	Morphable
	isTaggableTaggableParent()
}

// isTaggableTaggableParent marks *Post as an
// allowed parent of Taggable on the "taggable" relation.
// Generated only for types listed in the schema's AllowedTypes; this is
// the entire mechanism behind the compile-time restriction.
func (*Post) isTaggableTaggableParent() {}

// isTaggableTaggableParent marks *Video as an
// allowed parent of Taggable on the "taggable" relation.
// Generated only for types listed in the schema's AllowedTypes; this is
// the entire mechanism behind the compile-time restriction.
func (*Video) isTaggableTaggableParent() {}

// SetTarget sets the polymorphic parent on the Create builder.
// Accepts only types declared in the schema's AllowedTypes — see
// AnnotationTargetParent.
func (c *AnnotationCreate) SetTarget(p AnnotationTargetParent) *AnnotationCreate {
	return c.SetTargetID(p.MorphID()).SetTargetType(annotation.TargetType(string(p.MorphKey())))
}

// SetTarget sets the polymorphic parent on the Update builder.
func (u *AnnotationUpdate) SetTarget(p AnnotationTargetParent) *AnnotationUpdate {
	return u.SetTargetID(p.MorphID()).SetTargetType(annotation.TargetType(string(p.MorphKey())))
}

// ClearTarget clears the polymorphic parent on the Update builder.
func (u *AnnotationUpdate) ClearTarget() *AnnotationUpdate {
	return u.ClearTargetID().ClearTargetType()
}

// SetTarget sets the polymorphic parent on the single-row UpdateOne.
func (u *AnnotationUpdateOne) SetTarget(p AnnotationTargetParent) *AnnotationUpdateOne {
	return u.SetTargetID(p.MorphID()).SetTargetType(annotation.TargetType(string(p.MorphKey())))
}

// ClearTarget clears the polymorphic parent on the single-row UpdateOne.
func (u *AnnotationUpdateOne) ClearTarget() *AnnotationUpdateOne {
	return u.ClearTargetID().ClearTargetType()
}

// SetTarget sets the polymorphic parent on the Mutation.
func (m *AnnotationMutation) SetTarget(p AnnotationTargetParent) *AnnotationMutation {
	m.SetTargetID(p.MorphID())
	m.SetTargetType(annotation.TargetType(string(p.MorphKey())))
	return m
}

// ClearTarget clears the polymorphic parent on the Mutation.
func (m *AnnotationMutation) ClearTarget() *AnnotationMutation {
	m.ClearTargetID()
	m.ClearTargetType()
	return m
}

// AnnotationTargetIs returns a predicate that matches Annotation
// rows pointing at the given parent — both the id and the morph type must
// match. The parent is restricted to the AllowedTypes via the sealed
// AnnotationTargetParent interface.
//
//	client.Annotation.Query().Where(AnnotationTargetIs(post)).All(ctx)
func AnnotationTargetIs(p AnnotationTargetParent) predicate.Annotation {
	return annotation.And(
		annotation.TargetIDEQ(p.MorphID()),
		annotation.TargetTypeEQ(annotation.TargetType(string(p.MorphKey()))),
	)
}

// AnnotationTargetIsType returns a predicate that matches
// Annotation rows whose polymorphic parent is *any instance* of the given
// morph type. Accepts only the codegen-emitted MorphKey constants.
//
//	client.Annotation.Query().
//	    Where(AnnotationTargetIsType(PostMorphKey)).
//	    All(ctx)
func AnnotationTargetIsType(k MorphKey) predicate.Annotation {
	return annotation.TargetTypeEQ(annotation.TargetType(string(k)))
}

// AnnotationTargetOnDocument returns a predicate.Annotation
// matching rows whose polymorphic "target" parent is a
// Document satisfying every supplied predicate. Zero predicates
// match every Annotation pointing at a Document, regardless of
// the parent's columns.
//
// Laravel equivalent:
//
//	Comment::whereHasMorph('target', [Document::class], fn ($q) => $q->where('approved', true))
//
// entpoly:
//
//	client.Annotation.Query().Where(
//	    AnnotationTargetOnDocument(document.ApprovedEQ(true)),
//	).All(ctx)
func AnnotationTargetOnDocument(preds ...predicate.Document) predicate.Annotation {
	return predicate.Annotation(func(s *sql.Selector) {
		sub := sql.Select(document.FieldID).From(sql.Table(document.Table))
		for _, p := range preds {
			p(sub)
		}
		s.Where(sql.And(
			sql.EQ(s.C(annotation.FieldTargetType), string(DocumentMorphKey)),
			sql.In(s.C(annotation.FieldTargetID), sub),
		))
	})
}

// AnnotationTargetOnReport returns a predicate.Annotation
// matching rows whose polymorphic "target" parent is a
// Report satisfying every supplied predicate. Zero predicates
// match every Annotation pointing at a Report, regardless of
// the parent's columns.
//
// Laravel equivalent:
//
//	Comment::whereHasMorph('target', [Report::class], fn ($q) => $q->where('approved', true))
//
// entpoly:
//
//	client.Annotation.Query().Where(
//	    AnnotationTargetOnReport(report.ApprovedEQ(true)),
//	).All(ctx)
func AnnotationTargetOnReport(preds ...predicate.Report) predicate.Annotation {
	return predicate.Annotation(func(s *sql.Selector) {
		sub := sql.Select(report.FieldID).From(sql.Table(report.Table))
		for _, p := range preds {
			p(sub)
		}
		s.Where(sql.And(
			sql.EQ(s.C(annotation.FieldTargetType), string(ReportMorphKey)),
			sql.In(s.C(annotation.FieldTargetID), sub),
		))
	})
}

// QueryTarget resolves the polymorphic parent of this Annotation
// and returns it through the sealed-interface return type. The caller
// type-switches on the result to recover the concrete parent — only the
// types listed in MorphTo's AllowedTypes are possible branches.
//
// Laravel equivalent:
//
//	$commentable = $comment->commentable;  // Post | Video | null
//
// entpoly:
//
//	parent, err := comment.QueryTarget(ctx)
//	if err != nil { return err }
//	switch p := parent.(type) {
//	case *Document:
//	    // use p as *Document
//	case *Report:
//	    // use p as *Report
//	case nil:
//	    // no parent set
//	}
//
// Returns (nil, nil) when the discriminator pair is unset. Returns a
// typed error when the persisted morph type is not in the allowed set
// (e.g. data written by a different schema version or a bypass via raw
// SQL despite the DB enum constraint).
func (c *Annotation) QueryTarget(ctx context.Context) (AnnotationTargetParent, error) {
	if c.TargetID == nil || c.TargetType == nil {
		return nil, nil
	}
	switch *c.TargetType {
	case annotation.TargetType(string(DocumentMorphKey)):
		id, err := uuid.Parse(*c.TargetID)
		if err != nil {
			return nil, fmt.Errorf("entpoly: parse Document id %q: %w", *c.TargetID, err)
		}
		return NewDocumentClient(c.config).Get(ctx, id)
	case annotation.TargetType(string(ReportMorphKey)):
		id, err := uuid.Parse(*c.TargetID)
		if err != nil {
			return nil, fmt.Errorf("entpoly: parse Report id %q: %w", *c.TargetID, err)
		}
		return NewReportClient(c.config).Get(ctx, id)
	default:
		return nil, fmt.Errorf("entpoly: unknown morph type %q for Annotation.target", *c.TargetType)
	}
}

// SetSecondary sets the polymorphic parent on the Create builder.
// Accepts only types declared in the schema's AllowedTypes — see
// AnnotationSecondaryParent.
func (c *AnnotationCreate) SetSecondary(p AnnotationSecondaryParent) *AnnotationCreate {
	return c.SetSecondaryID(p.MorphID()).SetSecondaryType(annotation.SecondaryType(string(p.MorphKey())))
}

// SetSecondary sets the polymorphic parent on the Update builder.
func (u *AnnotationUpdate) SetSecondary(p AnnotationSecondaryParent) *AnnotationUpdate {
	return u.SetSecondaryID(p.MorphID()).SetSecondaryType(annotation.SecondaryType(string(p.MorphKey())))
}

// ClearSecondary clears the polymorphic parent on the Update builder.
func (u *AnnotationUpdate) ClearSecondary() *AnnotationUpdate {
	return u.ClearSecondaryID().ClearSecondaryType()
}

// SetSecondary sets the polymorphic parent on the single-row UpdateOne.
func (u *AnnotationUpdateOne) SetSecondary(p AnnotationSecondaryParent) *AnnotationUpdateOne {
	return u.SetSecondaryID(p.MorphID()).SetSecondaryType(annotation.SecondaryType(string(p.MorphKey())))
}

// ClearSecondary clears the polymorphic parent on the single-row UpdateOne.
func (u *AnnotationUpdateOne) ClearSecondary() *AnnotationUpdateOne {
	return u.ClearSecondaryID().ClearSecondaryType()
}

// SetSecondary sets the polymorphic parent on the Mutation.
func (m *AnnotationMutation) SetSecondary(p AnnotationSecondaryParent) *AnnotationMutation {
	m.SetSecondaryID(p.MorphID())
	m.SetSecondaryType(annotation.SecondaryType(string(p.MorphKey())))
	return m
}

// ClearSecondary clears the polymorphic parent on the Mutation.
func (m *AnnotationMutation) ClearSecondary() *AnnotationMutation {
	m.ClearSecondaryID()
	m.ClearSecondaryType()
	return m
}

// AnnotationSecondaryIs returns a predicate that matches Annotation
// rows pointing at the given parent — both the id and the morph type must
// match. The parent is restricted to the AllowedTypes via the sealed
// AnnotationSecondaryParent interface.
//
//	client.Annotation.Query().Where(AnnotationSecondaryIs(post)).All(ctx)
func AnnotationSecondaryIs(p AnnotationSecondaryParent) predicate.Annotation {
	return annotation.And(
		annotation.SecondaryIDEQ(p.MorphID()),
		annotation.SecondaryTypeEQ(annotation.SecondaryType(string(p.MorphKey()))),
	)
}

// AnnotationSecondaryIsType returns a predicate that matches
// Annotation rows whose polymorphic parent is *any instance* of the given
// morph type. Accepts only the codegen-emitted MorphKey constants.
//
//	client.Annotation.Query().
//	    Where(AnnotationSecondaryIsType(PostMorphKey)).
//	    All(ctx)
func AnnotationSecondaryIsType(k MorphKey) predicate.Annotation {
	return annotation.SecondaryTypeEQ(annotation.SecondaryType(string(k)))
}

// AnnotationSecondaryOnPost returns a predicate.Annotation
// matching rows whose polymorphic "secondary" parent is a
// Post satisfying every supplied predicate. Zero predicates
// match every Annotation pointing at a Post, regardless of
// the parent's columns.
//
// Laravel equivalent:
//
//	Comment::whereHasMorph('secondary', [Post::class], fn ($q) => $q->where('approved', true))
//
// entpoly:
//
//	client.Annotation.Query().Where(
//	    AnnotationSecondaryOnPost(post.ApprovedEQ(true)),
//	).All(ctx)
func AnnotationSecondaryOnPost(preds ...predicate.Post) predicate.Annotation {
	return predicate.Annotation(func(s *sql.Selector) {
		sub := sql.Select(post.FieldID).From(sql.Table(post.Table))
		for _, p := range preds {
			p(sub)
		}
		s.Where(sql.And(
			sql.EQ(s.C(annotation.FieldSecondaryType), string(PostMorphKey)),
			sql.In(s.C(annotation.FieldSecondaryID), sub),
		))
	})
}

// AnnotationSecondaryOnVideo returns a predicate.Annotation
// matching rows whose polymorphic "secondary" parent is a
// Video satisfying every supplied predicate. Zero predicates
// match every Annotation pointing at a Video, regardless of
// the parent's columns.
//
// Laravel equivalent:
//
//	Comment::whereHasMorph('secondary', [Video::class], fn ($q) => $q->where('approved', true))
//
// entpoly:
//
//	client.Annotation.Query().Where(
//	    AnnotationSecondaryOnVideo(video.ApprovedEQ(true)),
//	).All(ctx)
func AnnotationSecondaryOnVideo(preds ...predicate.Video) predicate.Annotation {
	return predicate.Annotation(func(s *sql.Selector) {
		sub := sql.Select(video.FieldID).From(sql.Table(video.Table))
		for _, p := range preds {
			p(sub)
		}
		s.Where(sql.And(
			sql.EQ(s.C(annotation.FieldSecondaryType), string(VideoMorphKey)),
			sql.In(s.C(annotation.FieldSecondaryID), sub),
		))
	})
}

// QuerySecondary resolves the polymorphic parent of this Annotation
// and returns it through the sealed-interface return type. The caller
// type-switches on the result to recover the concrete parent — only the
// types listed in MorphTo's AllowedTypes are possible branches.
//
// Laravel equivalent:
//
//	$commentable = $comment->commentable;  // Post | Video | null
//
// entpoly:
//
//	parent, err := comment.QuerySecondary(ctx)
//	if err != nil { return err }
//	switch p := parent.(type) {
//	case *Post:
//	    // use p as *Post
//	case *Video:
//	    // use p as *Video
//	case nil:
//	    // no parent set
//	}
//
// Returns (nil, nil) when the discriminator pair is unset. Returns a
// typed error when the persisted morph type is not in the allowed set
// (e.g. data written by a different schema version or a bypass via raw
// SQL despite the DB enum constraint).
func (c *Annotation) QuerySecondary(ctx context.Context) (AnnotationSecondaryParent, error) {
	if c.SecondaryID == nil || c.SecondaryType == nil {
		return nil, nil
	}
	switch *c.SecondaryType {
	case annotation.SecondaryType(string(PostMorphKey)):
		id, err := strconv.Atoi(*c.SecondaryID)
		if err != nil {
			return nil, fmt.Errorf("entpoly: parse Post id %q: %w", *c.SecondaryID, err)
		}
		return NewPostClient(c.config).Get(ctx, id)
	case annotation.SecondaryType(string(VideoMorphKey)):
		id, err := strconv.Atoi(*c.SecondaryID)
		if err != nil {
			return nil, fmt.Errorf("entpoly: parse Video id %q: %w", *c.SecondaryID, err)
		}
		return NewVideoClient(c.config).Get(ctx, id)
	default:
		return nil, fmt.Errorf("entpoly: unknown morph type %q for Annotation.secondary", *c.SecondaryType)
	}
}

// SetCommentable sets the polymorphic parent on the Create builder.
// Accepts only types declared in the schema's AllowedTypes — see
// CommentCommentableParent.
func (c *CommentCreate) SetCommentable(p CommentCommentableParent) *CommentCreate {
	return c.SetCommentableID(p.MorphID()).SetCommentableType(comment.CommentableType(string(p.MorphKey())))
}

// SetCommentable sets the polymorphic parent on the Update builder.
func (u *CommentUpdate) SetCommentable(p CommentCommentableParent) *CommentUpdate {
	return u.SetCommentableID(p.MorphID()).SetCommentableType(comment.CommentableType(string(p.MorphKey())))
}

// ClearCommentable clears the polymorphic parent on the Update builder.
func (u *CommentUpdate) ClearCommentable() *CommentUpdate {
	return u.ClearCommentableID().ClearCommentableType()
}

// SetCommentable sets the polymorphic parent on the single-row UpdateOne.
func (u *CommentUpdateOne) SetCommentable(p CommentCommentableParent) *CommentUpdateOne {
	return u.SetCommentableID(p.MorphID()).SetCommentableType(comment.CommentableType(string(p.MorphKey())))
}

// ClearCommentable clears the polymorphic parent on the single-row UpdateOne.
func (u *CommentUpdateOne) ClearCommentable() *CommentUpdateOne {
	return u.ClearCommentableID().ClearCommentableType()
}

// SetCommentable sets the polymorphic parent on the Mutation.
func (m *CommentMutation) SetCommentable(p CommentCommentableParent) *CommentMutation {
	m.SetCommentableID(p.MorphID())
	m.SetCommentableType(comment.CommentableType(string(p.MorphKey())))
	return m
}

// ClearCommentable clears the polymorphic parent on the Mutation.
func (m *CommentMutation) ClearCommentable() *CommentMutation {
	m.ClearCommentableID()
	m.ClearCommentableType()
	return m
}

// CommentCommentableIs returns a predicate that matches Comment
// rows pointing at the given parent — both the id and the morph type must
// match. The parent is restricted to the AllowedTypes via the sealed
// CommentCommentableParent interface.
//
//	client.Comment.Query().Where(CommentCommentableIs(post)).All(ctx)
func CommentCommentableIs(p CommentCommentableParent) predicate.Comment {
	return comment.And(
		comment.CommentableIDEQ(p.MorphID()),
		comment.CommentableTypeEQ(comment.CommentableType(string(p.MorphKey()))),
	)
}

// CommentCommentableIsType returns a predicate that matches
// Comment rows whose polymorphic parent is *any instance* of the given
// morph type. Accepts only the codegen-emitted MorphKey constants.
//
//	client.Comment.Query().
//	    Where(CommentCommentableIsType(PostMorphKey)).
//	    All(ctx)
func CommentCommentableIsType(k MorphKey) predicate.Comment {
	return comment.CommentableTypeEQ(comment.CommentableType(string(k)))
}

// CommentCommentableOnPost returns a predicate.Comment
// matching rows whose polymorphic "commentable" parent is a
// Post satisfying every supplied predicate. Zero predicates
// match every Comment pointing at a Post, regardless of
// the parent's columns.
//
// Laravel equivalent:
//
//	Comment::whereHasMorph('commentable', [Post::class], fn ($q) => $q->where('approved', true))
//
// entpoly:
//
//	client.Comment.Query().Where(
//	    CommentCommentableOnPost(post.ApprovedEQ(true)),
//	).All(ctx)
//
// SoftDelete enabled: the sub-query also filters out Post rows
// whose DeletedAt is non-null, matching the read-side
// guarantees of QueryCommentable / WithCommentable.
func CommentCommentableOnPost(preds ...predicate.Post) predicate.Comment {
	return predicate.Comment(func(s *sql.Selector) {
		sub := sql.Select(post.FieldID).From(sql.Table(post.Table))
		for _, p := range preds {
			p(sub)
		}
		sub.Where(sql.IsNull(post.FieldDeletedAt))
		s.Where(sql.And(
			sql.EQ(s.C(comment.FieldCommentableType), string(PostMorphKey)),
			sql.In(s.C(comment.FieldCommentableID), sub),
		))
	})
}

// CommentCommentableOnVideo returns a predicate.Comment
// matching rows whose polymorphic "commentable" parent is a
// Video satisfying every supplied predicate. Zero predicates
// match every Comment pointing at a Video, regardless of
// the parent's columns.
//
// Laravel equivalent:
//
//	Comment::whereHasMorph('commentable', [Video::class], fn ($q) => $q->where('approved', true))
//
// entpoly:
//
//	client.Comment.Query().Where(
//	    CommentCommentableOnVideo(video.ApprovedEQ(true)),
//	).All(ctx)
func CommentCommentableOnVideo(preds ...predicate.Video) predicate.Comment {
	return predicate.Comment(func(s *sql.Selector) {
		sub := sql.Select(video.FieldID).From(sql.Table(video.Table))
		for _, p := range preds {
			p(sub)
		}
		s.Where(sql.And(
			sql.EQ(s.C(comment.FieldCommentableType), string(VideoMorphKey)),
			sql.In(s.C(comment.FieldCommentableID), sub),
		))
	})
}

// CommentCommentableOnImage returns a predicate.Comment
// matching rows whose polymorphic "commentable" parent is a
// Image satisfying every supplied predicate. Zero predicates
// match every Comment pointing at a Image, regardless of
// the parent's columns.
//
// Laravel equivalent:
//
//	Comment::whereHasMorph('commentable', [Image::class], fn ($q) => $q->where('approved', true))
//
// entpoly:
//
//	client.Comment.Query().Where(
//	    CommentCommentableOnImage(image.ApprovedEQ(true)),
//	).All(ctx)
func CommentCommentableOnImage(preds ...predicate.Image) predicate.Comment {
	return predicate.Comment(func(s *sql.Selector) {
		sub := sql.Select(image.FieldID).From(sql.Table(image.Table))
		for _, p := range preds {
			p(sub)
		}
		s.Where(sql.And(
			sql.EQ(s.C(comment.FieldCommentableType), string(ImageMorphKey)),
			sql.In(s.C(comment.FieldCommentableID), sub),
		))
	})
}

// QueryCommentable resolves the polymorphic parent of this Comment
// and returns it through the sealed-interface return type. The caller
// type-switches on the result to recover the concrete parent — only the
// types listed in MorphTo's AllowedTypes are possible branches.
//
// Laravel equivalent:
//
//	$commentable = $comment->commentable;  // Post | Video | null
//
// entpoly:
//
//	parent, err := comment.QueryCommentable(ctx)
//	if err != nil { return err }
//	switch p := parent.(type) {
//	case *Post:
//	    // use p as *Post
//	case *Video:
//	    // use p as *Video
//	case *Image:
//	    // use p as *Image
//	case nil:
//	    // no parent set
//	}
//
// Returns (nil, nil) when the discriminator pair is unset. Returns a
// typed error when the persisted morph type is not in the allowed set
// (e.g. data written by a different schema version or a bypass via raw
// SQL despite the DB enum constraint).
func (c *Comment) QueryCommentable(ctx context.Context) (CommentCommentableParent, error) {
	if c.CommentableID == nil || c.CommentableType == nil {
		return nil, nil
	}
	switch *c.CommentableType {
	case comment.CommentableType(string(PostMorphKey)):
		id, err := strconv.Atoi(*c.CommentableID)
		if err != nil {
			return nil, fmt.Errorf("entpoly: parse Post id %q: %w", *c.CommentableID, err)
		}
		// Soft-delete filter: skip parents with non-null
		// DeletedAt; IsNotFound is the "soft-deleted or
		// genuinely missing" path — return (nil, nil) so the caller
		// can null-check without unwrapping the error type.
		res, qerr := NewPostClient(c.config).Query().
			Where(post.IDEQ(id), post.DeletedAtIsNil()).
			First(ctx)
		if qerr != nil {
			if IsNotFound(qerr) {
				return nil, nil
			}
			return nil, qerr
		}
		return res, nil
	case comment.CommentableType(string(VideoMorphKey)):
		id, err := strconv.Atoi(*c.CommentableID)
		if err != nil {
			return nil, fmt.Errorf("entpoly: parse Video id %q: %w", *c.CommentableID, err)
		}
		return NewVideoClient(c.config).Get(ctx, id)
	case comment.CommentableType(string(ImageMorphKey)):
		id, err := strconv.Atoi(*c.CommentableID)
		if err != nil {
			return nil, fmt.Errorf("entpoly: parse Image id %q: %w", *c.CommentableID, err)
		}
		return NewImageClient(c.config).Get(ctx, id)
	default:
		return nil, fmt.Errorf("entpoly: unknown morph type %q for Comment.commentable", *c.CommentableType)
	}
}

// SetEntity sets the polymorphic parent on the Create builder.
// Accepts only types declared in the schema's AllowedTypes — see
// EventEntityParent.
func (c *EventCreate) SetEntity(p EventEntityParent) *EventCreate {
	return c.SetEntityPk(p.MorphID()).SetEntityKind(event.EntityKind(string(p.MorphKey())))
}

// SetEntity sets the polymorphic parent on the Update builder.
func (u *EventUpdate) SetEntity(p EventEntityParent) *EventUpdate {
	return u.SetEntityPk(p.MorphID()).SetEntityKind(event.EntityKind(string(p.MorphKey())))
}

// ClearEntity clears the polymorphic parent on the Update builder.
func (u *EventUpdate) ClearEntity() *EventUpdate {
	return u.ClearEntityPk().ClearEntityKind()
}

// SetEntity sets the polymorphic parent on the single-row UpdateOne.
func (u *EventUpdateOne) SetEntity(p EventEntityParent) *EventUpdateOne {
	return u.SetEntityPk(p.MorphID()).SetEntityKind(event.EntityKind(string(p.MorphKey())))
}

// ClearEntity clears the polymorphic parent on the single-row UpdateOne.
func (u *EventUpdateOne) ClearEntity() *EventUpdateOne {
	return u.ClearEntityPk().ClearEntityKind()
}

// SetEntity sets the polymorphic parent on the Mutation.
func (m *EventMutation) SetEntity(p EventEntityParent) *EventMutation {
	m.SetEntityPk(p.MorphID())
	m.SetEntityKind(event.EntityKind(string(p.MorphKey())))
	return m
}

// ClearEntity clears the polymorphic parent on the Mutation.
func (m *EventMutation) ClearEntity() *EventMutation {
	m.ClearEntityPk()
	m.ClearEntityKind()
	return m
}

// EventEntityIs returns a predicate that matches Event
// rows pointing at the given parent — both the id and the morph type must
// match. The parent is restricted to the AllowedTypes via the sealed
// EventEntityParent interface.
//
//	client.Event.Query().Where(EventEntityIs(post)).All(ctx)
func EventEntityIs(p EventEntityParent) predicate.Event {
	return event.And(
		event.EntityPkEQ(p.MorphID()),
		event.EntityKindEQ(event.EntityKind(string(p.MorphKey()))),
	)
}

// EventEntityIsType returns a predicate that matches
// Event rows whose polymorphic parent is *any instance* of the given
// morph type. Accepts only the codegen-emitted MorphKey constants.
//
//	client.Event.Query().
//	    Where(EventEntityIsType(PostMorphKey)).
//	    All(ctx)
func EventEntityIsType(k MorphKey) predicate.Event {
	return event.EntityKindEQ(event.EntityKind(string(k)))
}

// EventEntityOnPost returns a predicate.Event
// matching rows whose polymorphic "entity" parent is a
// Post satisfying every supplied predicate. Zero predicates
// match every Event pointing at a Post, regardless of
// the parent's columns.
//
// Laravel equivalent:
//
//	Comment::whereHasMorph('entity', [Post::class], fn ($q) => $q->where('approved', true))
//
// entpoly:
//
//	client.Event.Query().Where(
//	    EventEntityOnPost(post.ApprovedEQ(true)),
//	).All(ctx)
func EventEntityOnPost(preds ...predicate.Post) predicate.Event {
	return predicate.Event(func(s *sql.Selector) {
		sub := sql.Select(post.FieldID).From(sql.Table(post.Table))
		for _, p := range preds {
			p(sub)
		}
		s.Where(sql.And(
			sql.EQ(s.C(event.FieldEntityKind), string(PostMorphKey)),
			sql.In(s.C(event.FieldEntityPk), sub),
		))
	})
}

// EventEntityOnVideo returns a predicate.Event
// matching rows whose polymorphic "entity" parent is a
// Video satisfying every supplied predicate. Zero predicates
// match every Event pointing at a Video, regardless of
// the parent's columns.
//
// Laravel equivalent:
//
//	Comment::whereHasMorph('entity', [Video::class], fn ($q) => $q->where('approved', true))
//
// entpoly:
//
//	client.Event.Query().Where(
//	    EventEntityOnVideo(video.ApprovedEQ(true)),
//	).All(ctx)
func EventEntityOnVideo(preds ...predicate.Video) predicate.Event {
	return predicate.Event(func(s *sql.Selector) {
		sub := sql.Select(video.FieldID).From(sql.Table(video.Table))
		for _, p := range preds {
			p(sub)
		}
		s.Where(sql.And(
			sql.EQ(s.C(event.FieldEntityKind), string(VideoMorphKey)),
			sql.In(s.C(event.FieldEntityPk), sub),
		))
	})
}

// QueryEntity resolves the polymorphic parent of this Event
// and returns it through the sealed-interface return type. The caller
// type-switches on the result to recover the concrete parent — only the
// types listed in MorphTo's AllowedTypes are possible branches.
//
// Laravel equivalent:
//
//	$commentable = $comment->commentable;  // Post | Video | null
//
// entpoly:
//
//	parent, err := comment.QueryEntity(ctx)
//	if err != nil { return err }
//	switch p := parent.(type) {
//	case *Post:
//	    // use p as *Post
//	case *Video:
//	    // use p as *Video
//	case nil:
//	    // no parent set
//	}
//
// Returns (nil, nil) when the discriminator pair is unset. Returns a
// typed error when the persisted morph type is not in the allowed set
// (e.g. data written by a different schema version or a bypass via raw
// SQL despite the DB enum constraint).
func (c *Event) QueryEntity(ctx context.Context) (EventEntityParent, error) {
	if c.EntityPk == nil || c.EntityKind == nil {
		return nil, nil
	}
	switch *c.EntityKind {
	case event.EntityKind(string(PostMorphKey)):
		id, err := strconv.Atoi(*c.EntityPk)
		if err != nil {
			return nil, fmt.Errorf("entpoly: parse Post id %q: %w", *c.EntityPk, err)
		}
		return NewPostClient(c.config).Get(ctx, id)
	case event.EntityKind(string(VideoMorphKey)):
		id, err := strconv.Atoi(*c.EntityPk)
		if err != nil {
			return nil, fmt.Errorf("entpoly: parse Video id %q: %w", *c.EntityPk, err)
		}
		return NewVideoClient(c.config).Get(ctx, id)
	default:
		return nil, fmt.Errorf("entpoly: unknown morph type %q for Event.entity", *c.EntityKind)
	}
}

// SetParent sets the polymorphic parent on the Create builder.
// Accepts only types declared in the schema's AllowedTypes — see
// FolderParentParent.
func (c *FolderCreate) SetParent(p FolderParentParent) *FolderCreate {
	return c.SetParentID(p.MorphID()).SetParentType(folder.ParentType(string(p.MorphKey())))
}

// SetParent sets the polymorphic parent on the Update builder.
func (u *FolderUpdate) SetParent(p FolderParentParent) *FolderUpdate {
	return u.SetParentID(p.MorphID()).SetParentType(folder.ParentType(string(p.MorphKey())))
}

// ClearParent clears the polymorphic parent on the Update builder.
func (u *FolderUpdate) ClearParent() *FolderUpdate {
	return u.ClearParentID().ClearParentType()
}

// SetParent sets the polymorphic parent on the single-row UpdateOne.
func (u *FolderUpdateOne) SetParent(p FolderParentParent) *FolderUpdateOne {
	return u.SetParentID(p.MorphID()).SetParentType(folder.ParentType(string(p.MorphKey())))
}

// ClearParent clears the polymorphic parent on the single-row UpdateOne.
func (u *FolderUpdateOne) ClearParent() *FolderUpdateOne {
	return u.ClearParentID().ClearParentType()
}

// SetParent sets the polymorphic parent on the Mutation.
func (m *FolderMutation) SetParent(p FolderParentParent) *FolderMutation {
	m.SetParentID(p.MorphID())
	m.SetParentType(folder.ParentType(string(p.MorphKey())))
	return m
}

// ClearParent clears the polymorphic parent on the Mutation.
func (m *FolderMutation) ClearParent() *FolderMutation {
	m.ClearParentID()
	m.ClearParentType()
	return m
}

// FolderParentIs returns a predicate that matches Folder
// rows pointing at the given parent — both the id and the morph type must
// match. The parent is restricted to the AllowedTypes via the sealed
// FolderParentParent interface.
//
//	client.Folder.Query().Where(FolderParentIs(post)).All(ctx)
func FolderParentIs(p FolderParentParent) predicate.Folder {
	return folder.And(
		folder.ParentIDEQ(p.MorphID()),
		folder.ParentTypeEQ(folder.ParentType(string(p.MorphKey()))),
	)
}

// FolderParentIsType returns a predicate that matches
// Folder rows whose polymorphic parent is *any instance* of the given
// morph type. Accepts only the codegen-emitted MorphKey constants.
//
//	client.Folder.Query().
//	    Where(FolderParentIsType(PostMorphKey)).
//	    All(ctx)
func FolderParentIsType(k MorphKey) predicate.Folder {
	return folder.ParentTypeEQ(folder.ParentType(string(k)))
}

// FolderParentOnFolder returns a predicate.Folder
// matching rows whose polymorphic "parent" parent is a
// Folder satisfying every supplied predicate. Zero predicates
// match every Folder pointing at a Folder, regardless of
// the parent's columns.
//
// Laravel equivalent:
//
//	Comment::whereHasMorph('parent', [Folder::class], fn ($q) => $q->where('approved', true))
//
// entpoly:
//
//	client.Folder.Query().Where(
//	    FolderParentOnFolder(folder.ApprovedEQ(true)),
//	).All(ctx)
func FolderParentOnFolder(preds ...predicate.Folder) predicate.Folder {
	return predicate.Folder(func(s *sql.Selector) {
		sub := sql.Select(folder.FieldID).From(sql.Table(folder.Table))
		for _, p := range preds {
			p(sub)
		}
		s.Where(sql.And(
			sql.EQ(s.C(folder.FieldParentType), string(FolderMorphKey)),
			sql.In(s.C(folder.FieldParentID), sub),
		))
	})
}

// FolderParentOnDocument returns a predicate.Folder
// matching rows whose polymorphic "parent" parent is a
// Document satisfying every supplied predicate. Zero predicates
// match every Folder pointing at a Document, regardless of
// the parent's columns.
//
// Laravel equivalent:
//
//	Comment::whereHasMorph('parent', [Document::class], fn ($q) => $q->where('approved', true))
//
// entpoly:
//
//	client.Folder.Query().Where(
//	    FolderParentOnDocument(document.ApprovedEQ(true)),
//	).All(ctx)
func FolderParentOnDocument(preds ...predicate.Document) predicate.Folder {
	return predicate.Folder(func(s *sql.Selector) {
		sub := sql.Select(document.FieldID).From(sql.Table(document.Table))
		for _, p := range preds {
			p(sub)
		}
		s.Where(sql.And(
			sql.EQ(s.C(folder.FieldParentType), string(DocumentMorphKey)),
			sql.In(s.C(folder.FieldParentID), sub),
		))
	})
}

// QueryParent resolves the polymorphic parent of this Folder
// and returns it through the sealed-interface return type. The caller
// type-switches on the result to recover the concrete parent — only the
// types listed in MorphTo's AllowedTypes are possible branches.
//
// Laravel equivalent:
//
//	$commentable = $comment->commentable;  // Post | Video | null
//
// entpoly:
//
//	parent, err := comment.QueryParent(ctx)
//	if err != nil { return err }
//	switch p := parent.(type) {
//	case *Folder:
//	    // use p as *Folder
//	case *Document:
//	    // use p as *Document
//	case nil:
//	    // no parent set
//	}
//
// Returns (nil, nil) when the discriminator pair is unset. Returns a
// typed error when the persisted morph type is not in the allowed set
// (e.g. data written by a different schema version or a bypass via raw
// SQL despite the DB enum constraint).
func (c *Folder) QueryParent(ctx context.Context) (FolderParentParent, error) {
	if c.ParentID == nil || c.ParentType == nil {
		return nil, nil
	}
	switch *c.ParentType {
	case folder.ParentType(string(FolderMorphKey)):
		id, err := strconv.Atoi(*c.ParentID)
		if err != nil {
			return nil, fmt.Errorf("entpoly: parse Folder id %q: %w", *c.ParentID, err)
		}
		return NewFolderClient(c.config).Get(ctx, id)
	case folder.ParentType(string(DocumentMorphKey)):
		id, err := uuid.Parse(*c.ParentID)
		if err != nil {
			return nil, fmt.Errorf("entpoly: parse Document id %q: %w", *c.ParentID, err)
		}
		return NewDocumentClient(c.config).Get(ctx, id)
	default:
		return nil, fmt.Errorf("entpoly: unknown morph type %q for Folder.parent", *c.ParentType)
	}
}

// SetTaggable sets the polymorphic parent on the Create builder.
// Accepts only types declared in the schema's AllowedTypes — see
// TaggableTaggableParent.
func (c *TaggableCreate) SetTaggable(p TaggableTaggableParent) *TaggableCreate {
	return c.SetTaggableID(p.MorphID()).SetTaggableType(taggable.TaggableType(string(p.MorphKey())))
}

// SetTaggable sets the polymorphic parent on the Update builder.
func (u *TaggableUpdate) SetTaggable(p TaggableTaggableParent) *TaggableUpdate {
	return u.SetTaggableID(p.MorphID()).SetTaggableType(taggable.TaggableType(string(p.MorphKey())))
}

// ClearTaggable clears the polymorphic parent on the Update builder.
func (u *TaggableUpdate) ClearTaggable() *TaggableUpdate {
	return u.ClearTaggableID().ClearTaggableType()
}

// SetTaggable sets the polymorphic parent on the single-row UpdateOne.
func (u *TaggableUpdateOne) SetTaggable(p TaggableTaggableParent) *TaggableUpdateOne {
	return u.SetTaggableID(p.MorphID()).SetTaggableType(taggable.TaggableType(string(p.MorphKey())))
}

// ClearTaggable clears the polymorphic parent on the single-row UpdateOne.
func (u *TaggableUpdateOne) ClearTaggable() *TaggableUpdateOne {
	return u.ClearTaggableID().ClearTaggableType()
}

// SetTaggable sets the polymorphic parent on the Mutation.
func (m *TaggableMutation) SetTaggable(p TaggableTaggableParent) *TaggableMutation {
	m.SetTaggableID(p.MorphID())
	m.SetTaggableType(taggable.TaggableType(string(p.MorphKey())))
	return m
}

// ClearTaggable clears the polymorphic parent on the Mutation.
func (m *TaggableMutation) ClearTaggable() *TaggableMutation {
	m.ClearTaggableID()
	m.ClearTaggableType()
	return m
}

// TaggableTaggableIs returns a predicate that matches Taggable
// rows pointing at the given parent — both the id and the morph type must
// match. The parent is restricted to the AllowedTypes via the sealed
// TaggableTaggableParent interface.
//
//	client.Taggable.Query().Where(TaggableTaggableIs(post)).All(ctx)
func TaggableTaggableIs(p TaggableTaggableParent) predicate.Taggable {
	return taggable.And(
		taggable.TaggableIDEQ(p.MorphID()),
		taggable.TaggableTypeEQ(taggable.TaggableType(string(p.MorphKey()))),
	)
}

// TaggableTaggableIsType returns a predicate that matches
// Taggable rows whose polymorphic parent is *any instance* of the given
// morph type. Accepts only the codegen-emitted MorphKey constants.
//
//	client.Taggable.Query().
//	    Where(TaggableTaggableIsType(PostMorphKey)).
//	    All(ctx)
func TaggableTaggableIsType(k MorphKey) predicate.Taggable {
	return taggable.TaggableTypeEQ(taggable.TaggableType(string(k)))
}

// TaggableTaggableOnPost returns a predicate.Taggable
// matching rows whose polymorphic "taggable" parent is a
// Post satisfying every supplied predicate. Zero predicates
// match every Taggable pointing at a Post, regardless of
// the parent's columns.
//
// Laravel equivalent:
//
//	Comment::whereHasMorph('taggable', [Post::class], fn ($q) => $q->where('approved', true))
//
// entpoly:
//
//	client.Taggable.Query().Where(
//	    TaggableTaggableOnPost(post.ApprovedEQ(true)),
//	).All(ctx)
func TaggableTaggableOnPost(preds ...predicate.Post) predicate.Taggable {
	return predicate.Taggable(func(s *sql.Selector) {
		sub := sql.Select(post.FieldID).From(sql.Table(post.Table))
		for _, p := range preds {
			p(sub)
		}
		s.Where(sql.And(
			sql.EQ(s.C(taggable.FieldTaggableType), string(PostMorphKey)),
			sql.In(s.C(taggable.FieldTaggableID), sub),
		))
	})
}

// TaggableTaggableOnVideo returns a predicate.Taggable
// matching rows whose polymorphic "taggable" parent is a
// Video satisfying every supplied predicate. Zero predicates
// match every Taggable pointing at a Video, regardless of
// the parent's columns.
//
// Laravel equivalent:
//
//	Comment::whereHasMorph('taggable', [Video::class], fn ($q) => $q->where('approved', true))
//
// entpoly:
//
//	client.Taggable.Query().Where(
//	    TaggableTaggableOnVideo(video.ApprovedEQ(true)),
//	).All(ctx)
func TaggableTaggableOnVideo(preds ...predicate.Video) predicate.Taggable {
	return predicate.Taggable(func(s *sql.Selector) {
		sub := sql.Select(video.FieldID).From(sql.Table(video.Table))
		for _, p := range preds {
			p(sub)
		}
		s.Where(sql.And(
			sql.EQ(s.C(taggable.FieldTaggableType), string(VideoMorphKey)),
			sql.In(s.C(taggable.FieldTaggableID), sub),
		))
	})
}

// QueryTaggable resolves the polymorphic parent of this Taggable
// and returns it through the sealed-interface return type. The caller
// type-switches on the result to recover the concrete parent — only the
// types listed in MorphTo's AllowedTypes are possible branches.
//
// Laravel equivalent:
//
//	$commentable = $comment->commentable;  // Post | Video | null
//
// entpoly:
//
//	parent, err := comment.QueryTaggable(ctx)
//	if err != nil { return err }
//	switch p := parent.(type) {
//	case *Post:
//	    // use p as *Post
//	case *Video:
//	    // use p as *Video
//	case nil:
//	    // no parent set
//	}
//
// Returns (nil, nil) when the discriminator pair is unset. Returns a
// typed error when the persisted morph type is not in the allowed set
// (e.g. data written by a different schema version or a bypass via raw
// SQL despite the DB enum constraint).
func (c *Taggable) QueryTaggable(ctx context.Context) (TaggableTaggableParent, error) {
	if c.TaggableID == nil || c.TaggableType == nil {
		return nil, nil
	}
	switch *c.TaggableType {
	case taggable.TaggableType(string(PostMorphKey)):
		id, err := strconv.Atoi(*c.TaggableID)
		if err != nil {
			return nil, fmt.Errorf("entpoly: parse Post id %q: %w", *c.TaggableID, err)
		}
		return NewPostClient(c.config).Get(ctx, id)
	case taggable.TaggableType(string(VideoMorphKey)):
		id, err := strconv.Atoi(*c.TaggableID)
		if err != nil {
			return nil, fmt.Errorf("entpoly: parse Video id %q: %w", *c.TaggableID, err)
		}
		return NewVideoClient(c.config).Get(ctx, id)
	default:
		return nil, fmt.Errorf("entpoly: unknown morph type %q for Taggable.taggable", *c.TaggableType)
	}
}

// AnnotationTarget is the gqlgen-recognisable union type for
// Annotation.target. Alias of the sealed-interface
// AnnotationTargetParent; using the alias keeps the
// GraphQL-side name independent of the Go-side identifier.
type AnnotationTarget = AnnotationTargetParent

// IsAnnotationTarget marks *Document as a member of the
// AnnotationTarget union for gqlgen. The exported marker is in
// addition to the sealed isAnnotationTargetParent
// marker that powers compile-time SetTarget restriction.
func (*Document) IsAnnotationTarget() {}

// IsAnnotationTarget marks *Report as a member of the
// AnnotationTarget union for gqlgen. The exported marker is in
// addition to the sealed isAnnotationTargetParent
// marker that powers compile-time SetTarget restriction.
func (*Report) IsAnnotationTarget() {}

// GQLTarget is the gqlgen-resolver-ready accessor for
// Annotation.target. Thin wrapper around
// QueryTarget — exists so the resolver in your gqlgen
// project is a single-line forward:
//
//	func (r *annotationResolver) Target(ctx context.Context, c *Annotation) (AnnotationTarget, error) {
//	    return c.GQLTarget(ctx)
//	}
func (c *Annotation) GQLTarget(ctx context.Context) (AnnotationTarget, error) {
	return c.QueryTarget(ctx)
}

// Commentable is the gqlgen-recognisable union type for
// Comment.commentable. Alias of the sealed-interface
// CommentCommentableParent; using the alias keeps the
// GraphQL-side name independent of the Go-side identifier.
type Commentable = CommentCommentableParent

// IsCommentable marks *Post as a member of the
// Commentable union for gqlgen. The exported marker is in
// addition to the sealed isCommentCommentableParent
// marker that powers compile-time SetCommentable restriction.
func (*Post) IsCommentable() {}

// IsCommentable marks *Video as a member of the
// Commentable union for gqlgen. The exported marker is in
// addition to the sealed isCommentCommentableParent
// marker that powers compile-time SetCommentable restriction.
func (*Video) IsCommentable() {}

// IsCommentable marks *Image as a member of the
// Commentable union for gqlgen. The exported marker is in
// addition to the sealed isCommentCommentableParent
// marker that powers compile-time SetCommentable restriction.
func (*Image) IsCommentable() {}

// GQLCommentable is the gqlgen-resolver-ready accessor for
// Comment.commentable. Thin wrapper around
// QueryCommentable — exists so the resolver in your gqlgen
// project is a single-line forward:
//
//	func (r *commentResolver) Commentable(ctx context.Context, c *Comment) (Commentable, error) {
//	    return c.GQLCommentable(ctx)
//	}
func (c *Comment) GQLCommentable(ctx context.Context) (Commentable, error) {
	return c.QueryCommentable(ctx)
}

// QueryPosts returns all Post rows polymorphically
// attached to this Tag via the Taggable pivot. Reads
// the pivot rows that match (holder, target-morph-key), parses the
// stringified ids, and loads the corresponding Post rows in a
// single follow-up query.
//
// Laravel:    $posts = $tag->posts;
// entpoly:    posts, err := tag.QueryPosts(ctx)
func (h *Tag) QueryPosts(ctx context.Context) ([]*Post, error) {
	pivots, err := NewTaggableClient(h.config).Query().
		Where(
			taggable.TagIDEQ(h.ID),
			taggable.TaggableTypeEQ(taggable.TaggableType(string(PostMorphKey))),
		).
		All(ctx)
	if err != nil {
		return nil, err
	}
	if len(pivots) == 0 {
		return []*Post{}, nil
	}
	ids := make([]int, 0, len(pivots))
	for _, p := range pivots {
		if p.TaggableID == nil {
			continue
		}
		id, perr := strconv.Atoi(*p.TaggableID)
		if perr != nil {
			return nil, fmt.Errorf("entpoly: parse Post id %q in Taggable pivot: %w", *p.TaggableID, perr)
		}
		ids = append(ids, id)
	}
	return NewPostClient(h.config).Query().Where(post.IDIn(ids...)).All(ctx)
}

// QueryTags returns all Tag rows attached
// to this Post via the Taggable pivot. Inverse direction of
// QueryPosts above — auto-generated from the holder-side
// MorphedByMany declaration so Post does NOT need a matching
// schema declaration.
//
// Laravel:    $tags = $post->tags;
// entpoly:    tags, err := post.QueryTags(ctx)
func (p *Post) QueryTags(ctx context.Context) ([]*Tag, error) {
	pivots, err := NewTaggableClient(p.config).Query().
		Where(
			taggable.TaggableIDEQ(p.MorphID()),
			taggable.TaggableTypeEQ(taggable.TaggableType(string(PostMorphKey))),
		).
		All(ctx)
	if err != nil {
		return nil, err
	}
	if len(pivots) == 0 {
		return []*Tag{}, nil
	}
	// Pivot's holder FK is a regular ent field, NOT a stringified
	// polymorphic id — collect the typed values directly.
	ids := make([]int, 0, len(pivots))
	for _, pv := range pivots {
		ids = append(ids, pv.TagID)
	}
	return NewTagClient(p.config).Query().Where(tag.IDIn(ids...)).All(ctx)
}

// QueryVideos returns all Video rows polymorphically
// attached to this Tag via the Taggable pivot. Reads
// the pivot rows that match (holder, target-morph-key), parses the
// stringified ids, and loads the corresponding Video rows in a
// single follow-up query.
//
// Laravel:    $posts = $tag->posts;
// entpoly:    posts, err := tag.QueryVideos(ctx)
func (h *Tag) QueryVideos(ctx context.Context) ([]*Video, error) {
	pivots, err := NewTaggableClient(h.config).Query().
		Where(
			taggable.TagIDEQ(h.ID),
			taggable.TaggableTypeEQ(taggable.TaggableType(string(VideoMorphKey))),
		).
		All(ctx)
	if err != nil {
		return nil, err
	}
	if len(pivots) == 0 {
		return []*Video{}, nil
	}
	ids := make([]int, 0, len(pivots))
	for _, p := range pivots {
		if p.TaggableID == nil {
			continue
		}
		id, perr := strconv.Atoi(*p.TaggableID)
		if perr != nil {
			return nil, fmt.Errorf("entpoly: parse Video id %q in Taggable pivot: %w", *p.TaggableID, perr)
		}
		ids = append(ids, id)
	}
	return NewVideoClient(h.config).Query().Where(video.IDIn(ids...)).All(ctx)
}

// QueryTags returns all Tag rows attached
// to this Video via the Taggable pivot. Inverse direction of
// QueryVideos above — auto-generated from the holder-side
// MorphedByMany declaration so Video does NOT need a matching
// schema declaration.
//
// Laravel:    $tags = $post->tags;
// entpoly:    tags, err := post.QueryTags(ctx)
func (p *Video) QueryTags(ctx context.Context) ([]*Tag, error) {
	pivots, err := NewTaggableClient(p.config).Query().
		Where(
			taggable.TaggableIDEQ(p.MorphID()),
			taggable.TaggableTypeEQ(taggable.TaggableType(string(VideoMorphKey))),
		).
		All(ctx)
	if err != nil {
		return nil, err
	}
	if len(pivots) == 0 {
		return []*Tag{}, nil
	}
	// Pivot's holder FK is a regular ent field, NOT a stringified
	// polymorphic id — collect the typed values directly.
	ids := make([]int, 0, len(pivots))
	for _, pv := range pivots {
		ids = append(ids, pv.TagID)
	}
	return NewTagClient(p.config).Query().Where(tag.IDIn(ids...)).All(ctx)
}

// AnnotationTargetPreloadResult is the typed eager-load
// result returned by (AnnotationQuery).WithTarget().All(ctx).
// The Annotations slice is the same shape
// AnnotationQuery.All(ctx) would have returned; the Target
// map is keyed by Annotation.ID and holds each row's resolved
// polymorphic parent.
type AnnotationTargetPreloadResult struct {
	Annotations []*Annotation
	Target      map[uuid.UUID]AnnotationTargetParent
}

// AnnotationTargetLoader is the ent-native chainable
// eager-load object returned by (AnnotationQuery).WithTarget().
// Call .All(ctx) on it to run the batched query and receive a typed
// result. Mirrors ent's standard .WithX().All(ctx) idiom.
type AnnotationTargetLoader struct {
	q *AnnotationQuery
}

// WithTarget returns the typed eager-load chain for this query.
// Use over plain .All(ctx) on any list-page path to avoid the N+1
// problem inherent to per-row QueryTarget(ctx) calls.
//
//	r, err := client.Annotation.Query().Limit(50).WithTarget().All(ctx)
//	for _, child := range r.Annotations {
//	    parent := r.Target[child.ID]
//	    switch p := parent.(type) {
//	    case *Document: // typed *Document
//	    case *Report: // typed *Report
//	    case nil: // discriminator unset on this row
//	    }
//	}
//
// Total queries: 1 (children) + N (distinct parent types in the
// batch). Compared to lazy resolution via QueryTarget(ctx),
// which fires one parent query PER child, this is the standard
// N+1 fix.
func (q *AnnotationQuery) WithTarget() *AnnotationTargetLoader {
	return &AnnotationTargetLoader{q: q}
}

// All runs the Annotation query and eager-loads every row's
// polymorphic "target" parent in batched per-type queries.
func (l *AnnotationTargetLoader) All(ctx context.Context) (*AnnotationTargetPreloadResult, error) {
	children, err := l.q.All(ctx)
	if err != nil {
		return nil, err
	}
	result := &AnnotationTargetPreloadResult{
		Annotations: children,
		Target:      make(map[uuid.UUID]AnnotationTargetParent, len(children)),
	}
	if len(children) == 0 {
		return result, nil
	}

	// Per-parent-type buckets — collect both the child→childlist map
	// (so we can attach the loaded parent back to every child) and
	// the list of parent IDs to feed the batched IN(...) query.
	var documentIDs []uuid.UUID
	documentToChildren := map[uuid.UUID][]*Annotation{}
	var reportIDs []uuid.UUID
	reportToChildren := map[uuid.UUID][]*Annotation{}

	for _, ch := range children {
		if ch.TargetID == nil || ch.TargetType == nil {
			continue
		}
		switch *ch.TargetType {
		case annotation.TargetType(string(DocumentMorphKey)):
			pid, perr := uuid.Parse(*ch.TargetID)
			if perr != nil {
				return nil, fmt.Errorf("entpoly: AllWithTarget: parse Document id %q: %w", *ch.TargetID, perr)
			}
			documentIDs = append(documentIDs, pid)
			documentToChildren[pid] = append(documentToChildren[pid], ch)
		case annotation.TargetType(string(ReportMorphKey)):
			pid, perr := uuid.Parse(*ch.TargetID)
			if perr != nil {
				return nil, fmt.Errorf("entpoly: AllWithTarget: parse Report id %q: %w", *ch.TargetID, perr)
			}
			reportIDs = append(reportIDs, pid)
			reportToChildren[pid] = append(reportToChildren[pid], ch)
		}
	}
	if len(documentIDs) > 0 {
		parents, perr := NewDocumentClient(l.q.config).Query().
			Where(document.IDIn(documentIDs...)).
			All(ctx)
		if perr != nil {
			return nil, fmt.Errorf("entpoly: AllWithTarget: batch-load Document: %w", perr)
		}
		for _, p := range parents {
			for _, ch := range documentToChildren[p.ID] {
				result.Target[ch.ID] = p
			}
		}
	}
	if len(reportIDs) > 0 {
		parents, perr := NewReportClient(l.q.config).Query().
			Where(report.IDIn(reportIDs...)).
			All(ctx)
		if perr != nil {
			return nil, fmt.Errorf("entpoly: AllWithTarget: batch-load Report: %w", perr)
		}
		for _, p := range parents {
			for _, ch := range reportToChildren[p.ID] {
				result.Target[ch.ID] = p
			}
		}
	}

	return result, nil
}

// AnnotationSecondaryPreloadResult is the typed eager-load
// result returned by (AnnotationQuery).WithSecondary().All(ctx).
// The Annotations slice is the same shape
// AnnotationQuery.All(ctx) would have returned; the Secondary
// map is keyed by Annotation.ID and holds each row's resolved
// polymorphic parent.
type AnnotationSecondaryPreloadResult struct {
	Annotations []*Annotation
	Secondary   map[uuid.UUID]AnnotationSecondaryParent
}

// AnnotationSecondaryLoader is the ent-native chainable
// eager-load object returned by (AnnotationQuery).WithSecondary().
// Call .All(ctx) on it to run the batched query and receive a typed
// result. Mirrors ent's standard .WithX().All(ctx) idiom.
type AnnotationSecondaryLoader struct {
	q *AnnotationQuery
}

// WithSecondary returns the typed eager-load chain for this query.
// Use over plain .All(ctx) on any list-page path to avoid the N+1
// problem inherent to per-row QuerySecondary(ctx) calls.
//
//	r, err := client.Annotation.Query().Limit(50).WithSecondary().All(ctx)
//	for _, child := range r.Annotations {
//	    parent := r.Secondary[child.ID]
//	    switch p := parent.(type) {
//	    case *Post: // typed *Post
//	    case *Video: // typed *Video
//	    case nil: // discriminator unset on this row
//	    }
//	}
//
// Total queries: 1 (children) + N (distinct parent types in the
// batch). Compared to lazy resolution via QuerySecondary(ctx),
// which fires one parent query PER child, this is the standard
// N+1 fix.
func (q *AnnotationQuery) WithSecondary() *AnnotationSecondaryLoader {
	return &AnnotationSecondaryLoader{q: q}
}

// All runs the Annotation query and eager-loads every row's
// polymorphic "secondary" parent in batched per-type queries.
func (l *AnnotationSecondaryLoader) All(ctx context.Context) (*AnnotationSecondaryPreloadResult, error) {
	children, err := l.q.All(ctx)
	if err != nil {
		return nil, err
	}
	result := &AnnotationSecondaryPreloadResult{
		Annotations: children,
		Secondary:   make(map[uuid.UUID]AnnotationSecondaryParent, len(children)),
	}
	if len(children) == 0 {
		return result, nil
	}

	// Per-parent-type buckets — collect both the child→childlist map
	// (so we can attach the loaded parent back to every child) and
	// the list of parent IDs to feed the batched IN(...) query.
	var postIDs []int
	postToChildren := map[int][]*Annotation{}
	var videoIDs []int
	videoToChildren := map[int][]*Annotation{}

	for _, ch := range children {
		if ch.SecondaryID == nil || ch.SecondaryType == nil {
			continue
		}
		switch *ch.SecondaryType {
		case annotation.SecondaryType(string(PostMorphKey)):
			pid, perr := strconv.Atoi(*ch.SecondaryID)
			if perr != nil {
				return nil, fmt.Errorf("entpoly: AllWithSecondary: parse Post id %q: %w", *ch.SecondaryID, perr)
			}
			postIDs = append(postIDs, pid)
			postToChildren[pid] = append(postToChildren[pid], ch)
		case annotation.SecondaryType(string(VideoMorphKey)):
			pid, perr := strconv.Atoi(*ch.SecondaryID)
			if perr != nil {
				return nil, fmt.Errorf("entpoly: AllWithSecondary: parse Video id %q: %w", *ch.SecondaryID, perr)
			}
			videoIDs = append(videoIDs, pid)
			videoToChildren[pid] = append(videoToChildren[pid], ch)
		}
	}
	if len(postIDs) > 0 {
		parents, perr := NewPostClient(l.q.config).Query().
			Where(post.IDIn(postIDs...)).
			All(ctx)
		if perr != nil {
			return nil, fmt.Errorf("entpoly: AllWithSecondary: batch-load Post: %w", perr)
		}
		for _, p := range parents {
			for _, ch := range postToChildren[p.ID] {
				result.Secondary[ch.ID] = p
			}
		}
	}
	if len(videoIDs) > 0 {
		parents, perr := NewVideoClient(l.q.config).Query().
			Where(video.IDIn(videoIDs...)).
			All(ctx)
		if perr != nil {
			return nil, fmt.Errorf("entpoly: AllWithSecondary: batch-load Video: %w", perr)
		}
		for _, p := range parents {
			for _, ch := range videoToChildren[p.ID] {
				result.Secondary[ch.ID] = p
			}
		}
	}

	return result, nil
}

// CommentCommentablePreloadResult is the typed eager-load
// result returned by (CommentQuery).WithCommentable().All(ctx).
// The Comments slice is the same shape
// CommentQuery.All(ctx) would have returned; the Commentable
// map is keyed by Comment.ID and holds each row's resolved
// polymorphic parent.
type CommentCommentablePreloadResult struct {
	Comments    []*Comment
	Commentable map[int]CommentCommentableParent
}

// CommentCommentableLoader is the ent-native chainable
// eager-load object returned by (CommentQuery).WithCommentable().
// Call .All(ctx) on it to run the batched query and receive a typed
// result. Mirrors ent's standard .WithX().All(ctx) idiom.
type CommentCommentableLoader struct {
	q *CommentQuery
}

// WithCommentable returns the typed eager-load chain for this query.
// Use over plain .All(ctx) on any list-page path to avoid the N+1
// problem inherent to per-row QueryCommentable(ctx) calls.
//
//	r, err := client.Comment.Query().Limit(50).WithCommentable().All(ctx)
//	for _, child := range r.Comments {
//	    parent := r.Commentable[child.ID]
//	    switch p := parent.(type) {
//	    case *Post: // typed *Post
//	    case *Video: // typed *Video
//	    case *Image: // typed *Image
//	    case nil: // discriminator unset on this row
//	    }
//	}
//
// Total queries: 1 (children) + N (distinct parent types in the
// batch). Compared to lazy resolution via QueryCommentable(ctx),
// which fires one parent query PER child, this is the standard
// N+1 fix.
func (q *CommentQuery) WithCommentable() *CommentCommentableLoader {
	return &CommentCommentableLoader{q: q}
}

// All runs the Comment query and eager-loads every row's
// polymorphic "commentable" parent in batched per-type queries.
func (l *CommentCommentableLoader) All(ctx context.Context) (*CommentCommentablePreloadResult, error) {
	children, err := l.q.All(ctx)
	if err != nil {
		return nil, err
	}
	result := &CommentCommentablePreloadResult{
		Comments:    children,
		Commentable: make(map[int]CommentCommentableParent, len(children)),
	}
	if len(children) == 0 {
		return result, nil
	}

	// Per-parent-type buckets — collect both the child→childlist map
	// (so we can attach the loaded parent back to every child) and
	// the list of parent IDs to feed the batched IN(...) query.
	var postIDs []int
	postToChildren := map[int][]*Comment{}
	var videoIDs []int
	videoToChildren := map[int][]*Comment{}
	var imageIDs []int
	imageToChildren := map[int][]*Comment{}

	for _, ch := range children {
		if ch.CommentableID == nil || ch.CommentableType == nil {
			continue
		}
		switch *ch.CommentableType {
		case comment.CommentableType(string(PostMorphKey)):
			pid, perr := strconv.Atoi(*ch.CommentableID)
			if perr != nil {
				return nil, fmt.Errorf("entpoly: AllWithCommentable: parse Post id %q: %w", *ch.CommentableID, perr)
			}
			postIDs = append(postIDs, pid)
			postToChildren[pid] = append(postToChildren[pid], ch)
		case comment.CommentableType(string(VideoMorphKey)):
			pid, perr := strconv.Atoi(*ch.CommentableID)
			if perr != nil {
				return nil, fmt.Errorf("entpoly: AllWithCommentable: parse Video id %q: %w", *ch.CommentableID, perr)
			}
			videoIDs = append(videoIDs, pid)
			videoToChildren[pid] = append(videoToChildren[pid], ch)
		case comment.CommentableType(string(ImageMorphKey)):
			pid, perr := strconv.Atoi(*ch.CommentableID)
			if perr != nil {
				return nil, fmt.Errorf("entpoly: AllWithCommentable: parse Image id %q: %w", *ch.CommentableID, perr)
			}
			imageIDs = append(imageIDs, pid)
			imageToChildren[pid] = append(imageToChildren[pid], ch)
		}
	}
	if len(postIDs) > 0 {
		parents, perr := NewPostClient(l.q.config).Query().
			Where(post.IDIn(postIDs...), post.DeletedAtIsNil()).
			All(ctx)
		if perr != nil {
			return nil, fmt.Errorf("entpoly: AllWithCommentable: batch-load Post: %w", perr)
		}
		for _, p := range parents {
			for _, ch := range postToChildren[p.ID] {
				result.Commentable[ch.ID] = p
			}
		}
	}
	if len(videoIDs) > 0 {
		parents, perr := NewVideoClient(l.q.config).Query().
			Where(video.IDIn(videoIDs...)).
			All(ctx)
		if perr != nil {
			return nil, fmt.Errorf("entpoly: AllWithCommentable: batch-load Video: %w", perr)
		}
		for _, p := range parents {
			for _, ch := range videoToChildren[p.ID] {
				result.Commentable[ch.ID] = p
			}
		}
	}
	if len(imageIDs) > 0 {
		parents, perr := NewImageClient(l.q.config).Query().
			Where(image.IDIn(imageIDs...)).
			All(ctx)
		if perr != nil {
			return nil, fmt.Errorf("entpoly: AllWithCommentable: batch-load Image: %w", perr)
		}
		for _, p := range parents {
			for _, ch := range imageToChildren[p.ID] {
				result.Commentable[ch.ID] = p
			}
		}
	}

	return result, nil
}

// EventEntityPreloadResult is the typed eager-load
// result returned by (EventQuery).WithEntity().All(ctx).
// The Events slice is the same shape
// EventQuery.All(ctx) would have returned; the Entity
// map is keyed by Event.ID and holds each row's resolved
// polymorphic parent.
type EventEntityPreloadResult struct {
	Events []*Event
	Entity map[int]EventEntityParent
}

// EventEntityLoader is the ent-native chainable
// eager-load object returned by (EventQuery).WithEntity().
// Call .All(ctx) on it to run the batched query and receive a typed
// result. Mirrors ent's standard .WithX().All(ctx) idiom.
type EventEntityLoader struct {
	q *EventQuery
}

// WithEntity returns the typed eager-load chain for this query.
// Use over plain .All(ctx) on any list-page path to avoid the N+1
// problem inherent to per-row QueryEntity(ctx) calls.
//
//	r, err := client.Event.Query().Limit(50).WithEntity().All(ctx)
//	for _, child := range r.Events {
//	    parent := r.Entity[child.ID]
//	    switch p := parent.(type) {
//	    case *Post: // typed *Post
//	    case *Video: // typed *Video
//	    case nil: // discriminator unset on this row
//	    }
//	}
//
// Total queries: 1 (children) + N (distinct parent types in the
// batch). Compared to lazy resolution via QueryEntity(ctx),
// which fires one parent query PER child, this is the standard
// N+1 fix.
func (q *EventQuery) WithEntity() *EventEntityLoader {
	return &EventEntityLoader{q: q}
}

// All runs the Event query and eager-loads every row's
// polymorphic "entity" parent in batched per-type queries.
func (l *EventEntityLoader) All(ctx context.Context) (*EventEntityPreloadResult, error) {
	children, err := l.q.All(ctx)
	if err != nil {
		return nil, err
	}
	result := &EventEntityPreloadResult{
		Events: children,
		Entity: make(map[int]EventEntityParent, len(children)),
	}
	if len(children) == 0 {
		return result, nil
	}

	// Per-parent-type buckets — collect both the child→childlist map
	// (so we can attach the loaded parent back to every child) and
	// the list of parent IDs to feed the batched IN(...) query.
	var postIDs []int
	postToChildren := map[int][]*Event{}
	var videoIDs []int
	videoToChildren := map[int][]*Event{}

	for _, ch := range children {
		if ch.EntityPk == nil || ch.EntityKind == nil {
			continue
		}
		switch *ch.EntityKind {
		case event.EntityKind(string(PostMorphKey)):
			pid, perr := strconv.Atoi(*ch.EntityPk)
			if perr != nil {
				return nil, fmt.Errorf("entpoly: AllWithEntity: parse Post id %q: %w", *ch.EntityPk, perr)
			}
			postIDs = append(postIDs, pid)
			postToChildren[pid] = append(postToChildren[pid], ch)
		case event.EntityKind(string(VideoMorphKey)):
			pid, perr := strconv.Atoi(*ch.EntityPk)
			if perr != nil {
				return nil, fmt.Errorf("entpoly: AllWithEntity: parse Video id %q: %w", *ch.EntityPk, perr)
			}
			videoIDs = append(videoIDs, pid)
			videoToChildren[pid] = append(videoToChildren[pid], ch)
		}
	}
	if len(postIDs) > 0 {
		parents, perr := NewPostClient(l.q.config).Query().
			Where(post.IDIn(postIDs...)).
			All(ctx)
		if perr != nil {
			return nil, fmt.Errorf("entpoly: AllWithEntity: batch-load Post: %w", perr)
		}
		for _, p := range parents {
			for _, ch := range postToChildren[p.ID] {
				result.Entity[ch.ID] = p
			}
		}
	}
	if len(videoIDs) > 0 {
		parents, perr := NewVideoClient(l.q.config).Query().
			Where(video.IDIn(videoIDs...)).
			All(ctx)
		if perr != nil {
			return nil, fmt.Errorf("entpoly: AllWithEntity: batch-load Video: %w", perr)
		}
		for _, p := range parents {
			for _, ch := range videoToChildren[p.ID] {
				result.Entity[ch.ID] = p
			}
		}
	}

	return result, nil
}

// FolderParentPreloadResult is the typed eager-load
// result returned by (FolderQuery).WithParent().All(ctx).
// The Folders slice is the same shape
// FolderQuery.All(ctx) would have returned; the Parent
// map is keyed by Folder.ID and holds each row's resolved
// polymorphic parent.
type FolderParentPreloadResult struct {
	Folders []*Folder
	Parent  map[int]FolderParentParent
}

// FolderParentLoader is the ent-native chainable
// eager-load object returned by (FolderQuery).WithParent().
// Call .All(ctx) on it to run the batched query and receive a typed
// result. Mirrors ent's standard .WithX().All(ctx) idiom.
type FolderParentLoader struct {
	q *FolderQuery
}

// WithParent returns the typed eager-load chain for this query.
// Use over plain .All(ctx) on any list-page path to avoid the N+1
// problem inherent to per-row QueryParent(ctx) calls.
//
//	r, err := client.Folder.Query().Limit(50).WithParent().All(ctx)
//	for _, child := range r.Folders {
//	    parent := r.Parent[child.ID]
//	    switch p := parent.(type) {
//	    case *Folder: // typed *Folder
//	    case *Document: // typed *Document
//	    case nil: // discriminator unset on this row
//	    }
//	}
//
// Total queries: 1 (children) + N (distinct parent types in the
// batch). Compared to lazy resolution via QueryParent(ctx),
// which fires one parent query PER child, this is the standard
// N+1 fix.
func (q *FolderQuery) WithParent() *FolderParentLoader {
	return &FolderParentLoader{q: q}
}

// All runs the Folder query and eager-loads every row's
// polymorphic "parent" parent in batched per-type queries.
func (l *FolderParentLoader) All(ctx context.Context) (*FolderParentPreloadResult, error) {
	children, err := l.q.All(ctx)
	if err != nil {
		return nil, err
	}
	result := &FolderParentPreloadResult{
		Folders: children,
		Parent:  make(map[int]FolderParentParent, len(children)),
	}
	if len(children) == 0 {
		return result, nil
	}

	// Per-parent-type buckets — collect both the child→childlist map
	// (so we can attach the loaded parent back to every child) and
	// the list of parent IDs to feed the batched IN(...) query.
	var folderIDs []int
	folderToChildren := map[int][]*Folder{}
	var documentIDs []uuid.UUID
	documentToChildren := map[uuid.UUID][]*Folder{}

	for _, ch := range children {
		if ch.ParentID == nil || ch.ParentType == nil {
			continue
		}
		switch *ch.ParentType {
		case folder.ParentType(string(FolderMorphKey)):
			pid, perr := strconv.Atoi(*ch.ParentID)
			if perr != nil {
				return nil, fmt.Errorf("entpoly: AllWithParent: parse Folder id %q: %w", *ch.ParentID, perr)
			}
			folderIDs = append(folderIDs, pid)
			folderToChildren[pid] = append(folderToChildren[pid], ch)
		case folder.ParentType(string(DocumentMorphKey)):
			pid, perr := uuid.Parse(*ch.ParentID)
			if perr != nil {
				return nil, fmt.Errorf("entpoly: AllWithParent: parse Document id %q: %w", *ch.ParentID, perr)
			}
			documentIDs = append(documentIDs, pid)
			documentToChildren[pid] = append(documentToChildren[pid], ch)
		}
	}
	if len(folderIDs) > 0 {
		parents, perr := NewFolderClient(l.q.config).Query().
			Where(folder.IDIn(folderIDs...)).
			All(ctx)
		if perr != nil {
			return nil, fmt.Errorf("entpoly: AllWithParent: batch-load Folder: %w", perr)
		}
		for _, p := range parents {
			for _, ch := range folderToChildren[p.ID] {
				result.Parent[ch.ID] = p
			}
		}
	}
	if len(documentIDs) > 0 {
		parents, perr := NewDocumentClient(l.q.config).Query().
			Where(document.IDIn(documentIDs...)).
			All(ctx)
		if perr != nil {
			return nil, fmt.Errorf("entpoly: AllWithParent: batch-load Document: %w", perr)
		}
		for _, p := range parents {
			for _, ch := range documentToChildren[p.ID] {
				result.Parent[ch.ID] = p
			}
		}
	}

	return result, nil
}

// TaggableTaggablePreloadResult is the typed eager-load
// result returned by (TaggableQuery).WithTaggable().All(ctx).
// The Taggables slice is the same shape
// TaggableQuery.All(ctx) would have returned; the Taggable
// map is keyed by Taggable.ID and holds each row's resolved
// polymorphic parent.
type TaggableTaggablePreloadResult struct {
	Taggables []*Taggable
	Taggable  map[int]TaggableTaggableParent
}

// TaggableTaggableLoader is the ent-native chainable
// eager-load object returned by (TaggableQuery).WithTaggable().
// Call .All(ctx) on it to run the batched query and receive a typed
// result. Mirrors ent's standard .WithX().All(ctx) idiom.
type TaggableTaggableLoader struct {
	q *TaggableQuery
}

// WithTaggable returns the typed eager-load chain for this query.
// Use over plain .All(ctx) on any list-page path to avoid the N+1
// problem inherent to per-row QueryTaggable(ctx) calls.
//
//	r, err := client.Taggable.Query().Limit(50).WithTaggable().All(ctx)
//	for _, child := range r.Taggables {
//	    parent := r.Taggable[child.ID]
//	    switch p := parent.(type) {
//	    case *Post: // typed *Post
//	    case *Video: // typed *Video
//	    case nil: // discriminator unset on this row
//	    }
//	}
//
// Total queries: 1 (children) + N (distinct parent types in the
// batch). Compared to lazy resolution via QueryTaggable(ctx),
// which fires one parent query PER child, this is the standard
// N+1 fix.
func (q *TaggableQuery) WithTaggable() *TaggableTaggableLoader {
	return &TaggableTaggableLoader{q: q}
}

// All runs the Taggable query and eager-loads every row's
// polymorphic "taggable" parent in batched per-type queries.
func (l *TaggableTaggableLoader) All(ctx context.Context) (*TaggableTaggablePreloadResult, error) {
	children, err := l.q.All(ctx)
	if err != nil {
		return nil, err
	}
	result := &TaggableTaggablePreloadResult{
		Taggables: children,
		Taggable:  make(map[int]TaggableTaggableParent, len(children)),
	}
	if len(children) == 0 {
		return result, nil
	}

	// Per-parent-type buckets — collect both the child→childlist map
	// (so we can attach the loaded parent back to every child) and
	// the list of parent IDs to feed the batched IN(...) query.
	var postIDs []int
	postToChildren := map[int][]*Taggable{}
	var videoIDs []int
	videoToChildren := map[int][]*Taggable{}

	for _, ch := range children {
		if ch.TaggableID == nil || ch.TaggableType == nil {
			continue
		}
		switch *ch.TaggableType {
		case taggable.TaggableType(string(PostMorphKey)):
			pid, perr := strconv.Atoi(*ch.TaggableID)
			if perr != nil {
				return nil, fmt.Errorf("entpoly: AllWithTaggable: parse Post id %q: %w", *ch.TaggableID, perr)
			}
			postIDs = append(postIDs, pid)
			postToChildren[pid] = append(postToChildren[pid], ch)
		case taggable.TaggableType(string(VideoMorphKey)):
			pid, perr := strconv.Atoi(*ch.TaggableID)
			if perr != nil {
				return nil, fmt.Errorf("entpoly: AllWithTaggable: parse Video id %q: %w", *ch.TaggableID, perr)
			}
			videoIDs = append(videoIDs, pid)
			videoToChildren[pid] = append(videoToChildren[pid], ch)
		}
	}
	if len(postIDs) > 0 {
		parents, perr := NewPostClient(l.q.config).Query().
			Where(post.IDIn(postIDs...)).
			All(ctx)
		if perr != nil {
			return nil, fmt.Errorf("entpoly: AllWithTaggable: batch-load Post: %w", perr)
		}
		for _, p := range parents {
			for _, ch := range postToChildren[p.ID] {
				result.Taggable[ch.ID] = p
			}
		}
	}
	if len(videoIDs) > 0 {
		parents, perr := NewVideoClient(l.q.config).Query().
			Where(video.IDIn(videoIDs...)).
			All(ctx)
		if perr != nil {
			return nil, fmt.Errorf("entpoly: AllWithTaggable: batch-load Video: %w", perr)
		}
		for _, p := range parents {
			for _, ch := range videoToChildren[p.ID] {
				result.Taggable[ch.ID] = p
			}
		}
	}

	return result, nil
}

// QueryComments returns a query builder for all Comment rows
// polymorphically attached to this Image via the
// "commentable" relation. The query is composable — chain .Where(),
// .Order(), .Limit(), .All(ctx), .First(ctx), etc.
//
//	// Laravel:    $comments = $post->comments;
//	// entpoly:    comments, _ := post.QueryComments().All(ctx)
//	//             page, _    := post.QueryComments().Limit(10).All(ctx)
func (p *Image) QueryComments() *CommentQuery {
	return NewCommentClient(p.config).Query().
		Where(
			comment.CommentableIDEQ(p.MorphID()),
			comment.CommentableTypeEQ(comment.CommentableType(string(p.MorphKey()))),
		)
}

// QueryComments returns a query builder for all Comment rows
// polymorphically attached to this Post via the
// "commentable" relation. The query is composable — chain .Where(),
// .Order(), .Limit(), .All(ctx), .First(ctx), etc.
//
//	// Laravel:    $comments = $post->comments;
//	// entpoly:    comments, _ := post.QueryComments().All(ctx)
//	//             page, _    := post.QueryComments().Limit(10).All(ctx)
func (p *Post) QueryComments() *CommentQuery {
	return NewCommentClient(p.config).Query().
		Where(
			comment.CommentableIDEQ(p.MorphID()),
			comment.CommentableTypeEQ(comment.CommentableType(string(p.MorphKey()))),
		)
}

// QueryComments returns a query builder for all Comment rows
// polymorphically attached to this Video via the
// "commentable" relation. The query is composable — chain .Where(),
// .Order(), .Limit(), .All(ctx), .First(ctx), etc.
//
//	// Laravel:    $comments = $post->comments;
//	// entpoly:    comments, _ := post.QueryComments().All(ctx)
//	//             page, _    := post.QueryComments().Limit(10).All(ctx)
func (p *Video) QueryComments() *CommentQuery {
	return NewCommentClient(p.config).Query().
		Where(
			comment.CommentableIDEQ(p.MorphID()),
			comment.CommentableTypeEQ(comment.CommentableType(string(p.MorphKey()))),
		)
}

// errEntPolyRequired is the sentinel returned by every Required()
// enforcement hook. Use errors.Is to detect.
var errEntPolyRequired = errors.New("entpoly: required polymorphic relation missing")

// RegisterPolyHooks installs the runtime hooks declared by MorphTo
// builder options (Required, Touch). Call once after creating the
// client, before any writes:
//
//	client := ent.NewClient(...)
//	ent.RegisterPolyHooks(client)
//
// Without this call the options are silently advisory:
//
//   - Required() will NOT reject incomplete writes.
//   - Touch() will NOT bump the parent timestamp.
//
// Hooks run in registration order; Required hooks come first so a
// rejected save short-circuits before any touch fires.
func RegisterPolyHooks(c *Client) {
	c.Comment.Use(commentCommentableRequiredHook())
	c.Comment.Use(commentCommentableTouchHook())
	c.Post.Use(commentCommentableCascadeOnPostDeleteHook())
	c.Video.Use(commentCommentableCascadeOnVideoDeleteHook())
	c.Image.Use(commentCommentableCascadeOnImageDeleteHook())
}

// commentCommentableRequiredHook returns the ent.Hook that enforces
// MorphTo("commentable").Required() at runtime. Wired automatically
// by RegisterPolyHooks; exposed individually so callers can install it
// on a sub-client (e.g. a tx) without registering every hook.
func commentCommentableRequiredHook() Hook {
	return func(next Mutator) Mutator {
		return MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
			cm, ok := m.(*CommentMutation)
			if !ok {
				return next.Mutate(ctx, m)
			}
			switch cm.Op() {
			case OpCreate:
				// On create both columns must be set. Use the
				// generated <Field>() (value, exists) getter.
				_, idOK := cm.CommentableID()
				_, tyOK := cm.CommentableType()
				if !idOK || !tyOK {
					return nil, fmt.Errorf("entpoly: Comment.commentable is required — "+
						"call SetCommentable(parent) before Save: %w", errEntPolyRequired)
				}
			case OpUpdate, OpUpdateOne:
				// On update reject explicit clears of either column.
				if cm.CommentableIDCleared() || cm.CommentableTypeCleared() {
					return nil, fmt.Errorf("entpoly: cannot ClearCommentable() — "+
						"Comment.commentable is Required(): %w", errEntPolyRequired)
				}
			}
			return next.Mutate(ctx, m)
		})
	}
}

// commentCommentableTouchHook returns the ent.Hook that
// implements Laravel-$touches semantics for MorphTo("commentable").Touch():
// after every successful Save of a Comment, the polymorphic
// parent's updated_at column is bumped to time.Now(). The
// parent update runs in the same logical operation; if it errors the
// child save is reported as failed.
//
// The hook is registered automatically by RegisterPolyHooks. Exposed
// individually so callers can install it on a sub-client (e.g. a tx).
//
// Each allowed parent type must declare a Time field named
// "updated_at". A missing field surfaces as an undefined-method
// compile error on SetUpdatedAt below.
func commentCommentableTouchHook() Hook {
	return func(next Mutator) Mutator {
		return MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
			// Run the child save first — only touch the parent if the
			// child write succeeded. Same ordering Laravel uses.
			v, err := next.Mutate(ctx, m)
			if err != nil {
				return v, err
			}
			cm, ok := m.(*CommentMutation)
			if !ok {
				return v, nil
			}
			// On Create the discriminator is in the mutation. On Update
			// the user typically did not re-set it, so fall back to the
			// OldX(ctx) accessor which loads the current row value
			// from the DB. The relation can still be unset (NULL), in
			// which case there is nothing to touch.
			idStr, idOK := cm.CommentableID()
			if !idOK {
				if oldID, oerr := cm.OldCommentableID(ctx); oerr == nil && oldID != nil {
					idStr = *oldID
					idOK = true
				}
			}
			tyVal, tyOK := cm.CommentableType()
			if !tyOK {
				if oldTy, oerr := cm.OldCommentableType(ctx); oerr == nil && oldTy != nil {
					tyVal = *oldTy
					tyOK = true
				}
			}
			if !idOK || !tyOK {
				return v, nil // discriminator unset; nothing to touch
			}
			now := time.Now()
			switch string(tyVal) {
			case string(PostMorphKey):
				pid, perr := strconv.Atoi(idStr)
				if perr != nil {
					return v, fmt.Errorf("entpoly: touch Post: parse id %q: %w", idStr, perr)
				}
				if _, terr := cm.Client().Post.UpdateOneID(pid).SetUpdatedAt(now).Save(ctx); terr != nil {
					return v, fmt.Errorf("entpoly: touch Post id %d: %w", pid, terr)
				}
			case string(VideoMorphKey):
				pid, perr := strconv.Atoi(idStr)
				if perr != nil {
					return v, fmt.Errorf("entpoly: touch Video: parse id %q: %w", idStr, perr)
				}
				if _, terr := cm.Client().Video.UpdateOneID(pid).SetUpdatedAt(now).Save(ctx); terr != nil {
					return v, fmt.Errorf("entpoly: touch Video id %d: %w", pid, terr)
				}
			case string(ImageMorphKey):
				pid, perr := strconv.Atoi(idStr)
				if perr != nil {
					return v, fmt.Errorf("entpoly: touch Image: parse id %q: %w", idStr, perr)
				}
				if _, terr := cm.Client().Image.UpdateOneID(pid).SetUpdatedAt(now).Save(ctx); terr != nil {
					return v, fmt.Errorf("entpoly: touch Image id %d: %w", pid, terr)
				}
			}
			return v, nil
		})
	}
}

// commentCommentableCascadeOnPostDeleteHook returns the
// pre-delete hook installed on Post by RegisterPolyHooks when
// MorphTo("commentable").Cascade() was declared on Comment.
// Deletes every Comment row whose "commentable" discriminator
// points at the Post being deleted, in the same logical op.
//
// Polymorphic columns cannot carry FK constraints, so the database
// will not cascade for us; this hook fills the gap. Order matters:
// children are removed BEFORE the parent so no orphan window opens.
func commentCommentableCascadeOnPostDeleteHook() Hook {
	return func(next Mutator) Mutator {
		return MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
			switch m.Op() {
			case OpDelete, OpDeleteOne:
			default:
				return next.Mutate(ctx, m)
			}
			pm, ok := m.(*PostMutation)
			if !ok {
				return next.Mutate(ctx, m)
			}
			ids, err := pm.IDs(ctx)
			if err != nil {
				return nil, fmt.Errorf("entpoly: cascade Comment.commentable: enumerate Post ids: %w", err)
			}
			for _, pid := range ids {
				if _, derr := pm.Client().Comment.Delete().Where(
					comment.CommentableTypeEQ(comment.CommentableType(string(PostMorphKey))),
					comment.CommentableIDEQ(fmt.Sprint(pid)),
				).Exec(ctx); derr != nil {
					return nil, fmt.Errorf("entpoly: cascade Comment.commentable for Post id %v: %w", pid, derr)
				}
			}
			return next.Mutate(ctx, m)
		})
	}
}

// commentCommentableCascadeOnVideoDeleteHook returns the
// pre-delete hook installed on Video by RegisterPolyHooks when
// MorphTo("commentable").Cascade() was declared on Comment.
// Deletes every Comment row whose "commentable" discriminator
// points at the Video being deleted, in the same logical op.
//
// Polymorphic columns cannot carry FK constraints, so the database
// will not cascade for us; this hook fills the gap. Order matters:
// children are removed BEFORE the parent so no orphan window opens.
func commentCommentableCascadeOnVideoDeleteHook() Hook {
	return func(next Mutator) Mutator {
		return MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
			switch m.Op() {
			case OpDelete, OpDeleteOne:
			default:
				return next.Mutate(ctx, m)
			}
			pm, ok := m.(*VideoMutation)
			if !ok {
				return next.Mutate(ctx, m)
			}
			ids, err := pm.IDs(ctx)
			if err != nil {
				return nil, fmt.Errorf("entpoly: cascade Comment.commentable: enumerate Video ids: %w", err)
			}
			for _, pid := range ids {
				if _, derr := pm.Client().Comment.Delete().Where(
					comment.CommentableTypeEQ(comment.CommentableType(string(VideoMorphKey))),
					comment.CommentableIDEQ(fmt.Sprint(pid)),
				).Exec(ctx); derr != nil {
					return nil, fmt.Errorf("entpoly: cascade Comment.commentable for Video id %v: %w", pid, derr)
				}
			}
			return next.Mutate(ctx, m)
		})
	}
}

// commentCommentableCascadeOnImageDeleteHook returns the
// pre-delete hook installed on Image by RegisterPolyHooks when
// MorphTo("commentable").Cascade() was declared on Comment.
// Deletes every Comment row whose "commentable" discriminator
// points at the Image being deleted, in the same logical op.
//
// Polymorphic columns cannot carry FK constraints, so the database
// will not cascade for us; this hook fills the gap. Order matters:
// children are removed BEFORE the parent so no orphan window opens.
func commentCommentableCascadeOnImageDeleteHook() Hook {
	return func(next Mutator) Mutator {
		return MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
			switch m.Op() {
			case OpDelete, OpDeleteOne:
			default:
				return next.Mutate(ctx, m)
			}
			pm, ok := m.(*ImageMutation)
			if !ok {
				return next.Mutate(ctx, m)
			}
			ids, err := pm.IDs(ctx)
			if err != nil {
				return nil, fmt.Errorf("entpoly: cascade Comment.commentable: enumerate Image ids: %w", err)
			}
			for _, pid := range ids {
				if _, derr := pm.Client().Comment.Delete().Where(
					comment.CommentableTypeEQ(comment.CommentableType(string(ImageMorphKey))),
					comment.CommentableIDEQ(fmt.Sprint(pid)),
				).Exec(ctx); derr != nil {
					return nil, fmt.Errorf("entpoly: cascade Comment.commentable for Image id %v: %w", pid, derr)
				}
			}
			return next.Mutate(ctx, m)
		})
	}
}
